spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop # 스프링부트에서 생성-드랍을 기본으로 사용
#    properties:
#      hibernate:
#        show_sql: true  # (system.out으로 출력하는 것임)
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace